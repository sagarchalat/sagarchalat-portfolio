import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import './pages/styles.css'; // If it's inside the 'pages' folder

ReactDOM.render(<App />, document.getElementById('root'));
